# Public base URL of the deployed site (used for canonical URLs, sitemap, OG, JSON-LD).
# Set this in Vercel → Project → Settings → Environment Variables.
NEXT_PUBLIC_SITE_URL=https://www.bursakaravanlibahce.com
# Wagtail JSON API base URL. Use http://localhost:8000/api locally.
NEXT_PUBLIC_CMS_API_URL=https://your-wagtail-app.onrender.com/api
